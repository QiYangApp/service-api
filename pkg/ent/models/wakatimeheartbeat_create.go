// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"ent/models/wakatimeheartbeat"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeHeartBeatCreate is the builder for creating a WakatimeHeartBeat entity.
type WakatimeHeartBeatCreate struct {
	config
	mutation *WakatimeHeartBeatMutation
	hooks    []Hook
}

// Mutation returns the WakatimeHeartBeatMutation object of the builder.
func (whbc *WakatimeHeartBeatCreate) Mutation() *WakatimeHeartBeatMutation {
	return whbc.mutation
}

// Save creates the WakatimeHeartBeat in the database.
func (whbc *WakatimeHeartBeatCreate) Save(ctx context.Context) (*WakatimeHeartBeat, error) {
	return withHooks(ctx, whbc.sqlSave, whbc.mutation, whbc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (whbc *WakatimeHeartBeatCreate) SaveX(ctx context.Context) *WakatimeHeartBeat {
	v, err := whbc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (whbc *WakatimeHeartBeatCreate) Exec(ctx context.Context) error {
	_, err := whbc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (whbc *WakatimeHeartBeatCreate) ExecX(ctx context.Context) {
	if err := whbc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (whbc *WakatimeHeartBeatCreate) check() error {
	return nil
}

func (whbc *WakatimeHeartBeatCreate) sqlSave(ctx context.Context) (*WakatimeHeartBeat, error) {
	if err := whbc.check(); err != nil {
		return nil, err
	}
	_node, _spec := whbc.createSpec()
	if err := sqlgraph.CreateNode(ctx, whbc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	whbc.mutation.id = &_node.ID
	whbc.mutation.done = true
	return _node, nil
}

func (whbc *WakatimeHeartBeatCreate) createSpec() (*WakatimeHeartBeat, *sqlgraph.CreateSpec) {
	var (
		_node = &WakatimeHeartBeat{config: whbc.config}
		_spec = sqlgraph.NewCreateSpec(wakatimeheartbeat.Table, sqlgraph.NewFieldSpec(wakatimeheartbeat.FieldID, field.TypeInt))
	)
	return _node, _spec
}

// WakatimeHeartBeatCreateBulk is the builder for creating many WakatimeHeartBeat entities in bulk.
type WakatimeHeartBeatCreateBulk struct {
	config
	err      error
	builders []*WakatimeHeartBeatCreate
}

// Save creates the WakatimeHeartBeat entities in the database.
func (whbcb *WakatimeHeartBeatCreateBulk) Save(ctx context.Context) ([]*WakatimeHeartBeat, error) {
	if whbcb.err != nil {
		return nil, whbcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(whbcb.builders))
	nodes := make([]*WakatimeHeartBeat, len(whbcb.builders))
	mutators := make([]Mutator, len(whbcb.builders))
	for i := range whbcb.builders {
		func(i int, root context.Context) {
			builder := whbcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WakatimeHeartBeatMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, whbcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, whbcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, whbcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (whbcb *WakatimeHeartBeatCreateBulk) SaveX(ctx context.Context) []*WakatimeHeartBeat {
	v, err := whbcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (whbcb *WakatimeHeartBeatCreateBulk) Exec(ctx context.Context) error {
	_, err := whbcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (whbcb *WakatimeHeartBeatCreateBulk) ExecX(ctx context.Context) {
	if err := whbcb.Exec(ctx); err != nil {
		panic(err)
	}
}
