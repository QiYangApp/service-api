// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"ent/models/predicate"
	"ent/models/wakatimeentity"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeEntityQuery is the builder for querying WakatimeEntity entities.
type WakatimeEntityQuery struct {
	config
	ctx        *QueryContext
	order      []wakatimeentity.OrderOption
	inters     []Interceptor
	predicates []predicate.WakatimeEntity
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WakatimeEntityQuery builder.
func (weq *WakatimeEntityQuery) Where(ps ...predicate.WakatimeEntity) *WakatimeEntityQuery {
	weq.predicates = append(weq.predicates, ps...)
	return weq
}

// Limit the number of records to be returned by this query.
func (weq *WakatimeEntityQuery) Limit(limit int) *WakatimeEntityQuery {
	weq.ctx.Limit = &limit
	return weq
}

// Offset to start from.
func (weq *WakatimeEntityQuery) Offset(offset int) *WakatimeEntityQuery {
	weq.ctx.Offset = &offset
	return weq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (weq *WakatimeEntityQuery) Unique(unique bool) *WakatimeEntityQuery {
	weq.ctx.Unique = &unique
	return weq
}

// Order specifies how the records should be ordered.
func (weq *WakatimeEntityQuery) Order(o ...wakatimeentity.OrderOption) *WakatimeEntityQuery {
	weq.order = append(weq.order, o...)
	return weq
}

// First returns the first WakatimeEntity entity from the query.
// Returns a *NotFoundError when no WakatimeEntity was found.
func (weq *WakatimeEntityQuery) First(ctx context.Context) (*WakatimeEntity, error) {
	nodes, err := weq.Limit(1).All(setContextOp(ctx, weq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wakatimeentity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (weq *WakatimeEntityQuery) FirstX(ctx context.Context) *WakatimeEntity {
	node, err := weq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WakatimeEntity ID from the query.
// Returns a *NotFoundError when no WakatimeEntity ID was found.
func (weq *WakatimeEntityQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = weq.Limit(1).IDs(setContextOp(ctx, weq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wakatimeentity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (weq *WakatimeEntityQuery) FirstIDX(ctx context.Context) int {
	id, err := weq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WakatimeEntity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WakatimeEntity entity is found.
// Returns a *NotFoundError when no WakatimeEntity entities are found.
func (weq *WakatimeEntityQuery) Only(ctx context.Context) (*WakatimeEntity, error) {
	nodes, err := weq.Limit(2).All(setContextOp(ctx, weq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wakatimeentity.Label}
	default:
		return nil, &NotSingularError{wakatimeentity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (weq *WakatimeEntityQuery) OnlyX(ctx context.Context) *WakatimeEntity {
	node, err := weq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WakatimeEntity ID in the query.
// Returns a *NotSingularError when more than one WakatimeEntity ID is found.
// Returns a *NotFoundError when no entities are found.
func (weq *WakatimeEntityQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = weq.Limit(2).IDs(setContextOp(ctx, weq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wakatimeentity.Label}
	default:
		err = &NotSingularError{wakatimeentity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (weq *WakatimeEntityQuery) OnlyIDX(ctx context.Context) int {
	id, err := weq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WakatimeEntities.
func (weq *WakatimeEntityQuery) All(ctx context.Context) ([]*WakatimeEntity, error) {
	ctx = setContextOp(ctx, weq.ctx, "All")
	if err := weq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WakatimeEntity, *WakatimeEntityQuery]()
	return withInterceptors[[]*WakatimeEntity](ctx, weq, qr, weq.inters)
}

// AllX is like All, but panics if an error occurs.
func (weq *WakatimeEntityQuery) AllX(ctx context.Context) []*WakatimeEntity {
	nodes, err := weq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WakatimeEntity IDs.
func (weq *WakatimeEntityQuery) IDs(ctx context.Context) (ids []int, err error) {
	if weq.ctx.Unique == nil && weq.path != nil {
		weq.Unique(true)
	}
	ctx = setContextOp(ctx, weq.ctx, "IDs")
	if err = weq.Select(wakatimeentity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (weq *WakatimeEntityQuery) IDsX(ctx context.Context) []int {
	ids, err := weq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (weq *WakatimeEntityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, weq.ctx, "Count")
	if err := weq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, weq, querierCount[*WakatimeEntityQuery](), weq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (weq *WakatimeEntityQuery) CountX(ctx context.Context) int {
	count, err := weq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (weq *WakatimeEntityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, weq.ctx, "Exist")
	switch _, err := weq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (weq *WakatimeEntityQuery) ExistX(ctx context.Context) bool {
	exist, err := weq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WakatimeEntityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (weq *WakatimeEntityQuery) Clone() *WakatimeEntityQuery {
	if weq == nil {
		return nil
	}
	return &WakatimeEntityQuery{
		config:     weq.config,
		ctx:        weq.ctx.Clone(),
		order:      append([]wakatimeentity.OrderOption{}, weq.order...),
		inters:     append([]Interceptor{}, weq.inters...),
		predicates: append([]predicate.WakatimeEntity{}, weq.predicates...),
		// clone intermediate query.
		sql:  weq.sql.Clone(),
		path: weq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (weq *WakatimeEntityQuery) GroupBy(field string, fields ...string) *WakatimeEntityGroupBy {
	weq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WakatimeEntityGroupBy{build: weq}
	grbuild.flds = &weq.ctx.Fields
	grbuild.label = wakatimeentity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (weq *WakatimeEntityQuery) Select(fields ...string) *WakatimeEntitySelect {
	weq.ctx.Fields = append(weq.ctx.Fields, fields...)
	sbuild := &WakatimeEntitySelect{WakatimeEntityQuery: weq}
	sbuild.label = wakatimeentity.Label
	sbuild.flds, sbuild.scan = &weq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WakatimeEntitySelect configured with the given aggregations.
func (weq *WakatimeEntityQuery) Aggregate(fns ...AggregateFunc) *WakatimeEntitySelect {
	return weq.Select().Aggregate(fns...)
}

func (weq *WakatimeEntityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range weq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, weq); err != nil {
				return err
			}
		}
	}
	for _, f := range weq.ctx.Fields {
		if !wakatimeentity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if weq.path != nil {
		prev, err := weq.path(ctx)
		if err != nil {
			return err
		}
		weq.sql = prev
	}
	return nil
}

func (weq *WakatimeEntityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WakatimeEntity, error) {
	var (
		nodes = []*WakatimeEntity{}
		_spec = weq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WakatimeEntity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WakatimeEntity{config: weq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, weq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (weq *WakatimeEntityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := weq.querySpec()
	_spec.Node.Columns = weq.ctx.Fields
	if len(weq.ctx.Fields) > 0 {
		_spec.Unique = weq.ctx.Unique != nil && *weq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, weq.driver, _spec)
}

func (weq *WakatimeEntityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wakatimeentity.Table, wakatimeentity.Columns, sqlgraph.NewFieldSpec(wakatimeentity.FieldID, field.TypeInt))
	_spec.From = weq.sql
	if unique := weq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if weq.path != nil {
		_spec.Unique = true
	}
	if fields := weq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wakatimeentity.FieldID)
		for i := range fields {
			if fields[i] != wakatimeentity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := weq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := weq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := weq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := weq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (weq *WakatimeEntityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(weq.driver.Dialect())
	t1 := builder.Table(wakatimeentity.Table)
	columns := weq.ctx.Fields
	if len(columns) == 0 {
		columns = wakatimeentity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if weq.sql != nil {
		selector = weq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if weq.ctx.Unique != nil && *weq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range weq.predicates {
		p(selector)
	}
	for _, p := range weq.order {
		p(selector)
	}
	if offset := weq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := weq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WakatimeEntityGroupBy is the group-by builder for WakatimeEntity entities.
type WakatimeEntityGroupBy struct {
	selector
	build *WakatimeEntityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wegb *WakatimeEntityGroupBy) Aggregate(fns ...AggregateFunc) *WakatimeEntityGroupBy {
	wegb.fns = append(wegb.fns, fns...)
	return wegb
}

// Scan applies the selector query and scans the result into the given value.
func (wegb *WakatimeEntityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wegb.build.ctx, "GroupBy")
	if err := wegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeEntityQuery, *WakatimeEntityGroupBy](ctx, wegb.build, wegb, wegb.build.inters, v)
}

func (wegb *WakatimeEntityGroupBy) sqlScan(ctx context.Context, root *WakatimeEntityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wegb.fns))
	for _, fn := range wegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wegb.flds)+len(wegb.fns))
		for _, f := range *wegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WakatimeEntitySelect is the builder for selecting fields of WakatimeEntity entities.
type WakatimeEntitySelect struct {
	*WakatimeEntityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wes *WakatimeEntitySelect) Aggregate(fns ...AggregateFunc) *WakatimeEntitySelect {
	wes.fns = append(wes.fns, fns...)
	return wes
}

// Scan applies the selector query and scans the result into the given value.
func (wes *WakatimeEntitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wes.ctx, "Select")
	if err := wes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeEntityQuery, *WakatimeEntitySelect](ctx, wes.WakatimeEntityQuery, wes, wes.inters, v)
}

func (wes *WakatimeEntitySelect) sqlScan(ctx context.Context, root *WakatimeEntityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wes.fns))
	for _, fn := range wes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
