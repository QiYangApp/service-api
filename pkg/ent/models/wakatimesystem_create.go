// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"ent/models/wakatimesystem"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeSystemCreate is the builder for creating a WakatimeSystem entity.
type WakatimeSystemCreate struct {
	config
	mutation *WakatimeSystemMutation
	hooks    []Hook
}

// Mutation returns the WakatimeSystemMutation object of the builder.
func (wsc *WakatimeSystemCreate) Mutation() *WakatimeSystemMutation {
	return wsc.mutation
}

// Save creates the WakatimeSystem in the database.
func (wsc *WakatimeSystemCreate) Save(ctx context.Context) (*WakatimeSystem, error) {
	return withHooks(ctx, wsc.sqlSave, wsc.mutation, wsc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (wsc *WakatimeSystemCreate) SaveX(ctx context.Context) *WakatimeSystem {
	v, err := wsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wsc *WakatimeSystemCreate) Exec(ctx context.Context) error {
	_, err := wsc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wsc *WakatimeSystemCreate) ExecX(ctx context.Context) {
	if err := wsc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wsc *WakatimeSystemCreate) check() error {
	return nil
}

func (wsc *WakatimeSystemCreate) sqlSave(ctx context.Context) (*WakatimeSystem, error) {
	if err := wsc.check(); err != nil {
		return nil, err
	}
	_node, _spec := wsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int64(id)
	wsc.mutation.id = &_node.ID
	wsc.mutation.done = true
	return _node, nil
}

func (wsc *WakatimeSystemCreate) createSpec() (*WakatimeSystem, *sqlgraph.CreateSpec) {
	var (
		_node = &WakatimeSystem{config: wsc.config}
		_spec = sqlgraph.NewCreateSpec(wakatimesystem.Table, sqlgraph.NewFieldSpec(wakatimesystem.FieldID, field.TypeInt64))
	)
	return _node, _spec
}

// WakatimeSystemCreateBulk is the builder for creating many WakatimeSystem entities in bulk.
type WakatimeSystemCreateBulk struct {
	config
	err      error
	builders []*WakatimeSystemCreate
}

// Save creates the WakatimeSystem entities in the database.
func (wscb *WakatimeSystemCreateBulk) Save(ctx context.Context) ([]*WakatimeSystem, error) {
	if wscb.err != nil {
		return nil, wscb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(wscb.builders))
	nodes := make([]*WakatimeSystem, len(wscb.builders))
	mutators := make([]Mutator, len(wscb.builders))
	for i := range wscb.builders {
		func(i int, root context.Context) {
			builder := wscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WakatimeSystemMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int64(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (wscb *WakatimeSystemCreateBulk) SaveX(ctx context.Context) []*WakatimeSystem {
	v, err := wscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wscb *WakatimeSystemCreateBulk) Exec(ctx context.Context) error {
	_, err := wscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wscb *WakatimeSystemCreateBulk) ExecX(ctx context.Context) {
	if err := wscb.Exec(ctx); err != nil {
		panic(err)
	}
}
