// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"ent/models/predicate"
	"ent/models/wakatimeduration"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeDurationQuery is the builder for querying WakatimeDuration entities.
type WakatimeDurationQuery struct {
	config
	ctx        *QueryContext
	order      []wakatimeduration.OrderOption
	inters     []Interceptor
	predicates []predicate.WakatimeDuration
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WakatimeDurationQuery builder.
func (wdq *WakatimeDurationQuery) Where(ps ...predicate.WakatimeDuration) *WakatimeDurationQuery {
	wdq.predicates = append(wdq.predicates, ps...)
	return wdq
}

// Limit the number of records to be returned by this query.
func (wdq *WakatimeDurationQuery) Limit(limit int) *WakatimeDurationQuery {
	wdq.ctx.Limit = &limit
	return wdq
}

// Offset to start from.
func (wdq *WakatimeDurationQuery) Offset(offset int) *WakatimeDurationQuery {
	wdq.ctx.Offset = &offset
	return wdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wdq *WakatimeDurationQuery) Unique(unique bool) *WakatimeDurationQuery {
	wdq.ctx.Unique = &unique
	return wdq
}

// Order specifies how the records should be ordered.
func (wdq *WakatimeDurationQuery) Order(o ...wakatimeduration.OrderOption) *WakatimeDurationQuery {
	wdq.order = append(wdq.order, o...)
	return wdq
}

// First returns the first WakatimeDuration entity from the query.
// Returns a *NotFoundError when no WakatimeDuration was found.
func (wdq *WakatimeDurationQuery) First(ctx context.Context) (*WakatimeDuration, error) {
	nodes, err := wdq.Limit(1).All(setContextOp(ctx, wdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wakatimeduration.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) FirstX(ctx context.Context) *WakatimeDuration {
	node, err := wdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WakatimeDuration ID from the query.
// Returns a *NotFoundError when no WakatimeDuration ID was found.
func (wdq *WakatimeDurationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wdq.Limit(1).IDs(setContextOp(ctx, wdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wakatimeduration.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) FirstIDX(ctx context.Context) int {
	id, err := wdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WakatimeDuration entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WakatimeDuration entity is found.
// Returns a *NotFoundError when no WakatimeDuration entities are found.
func (wdq *WakatimeDurationQuery) Only(ctx context.Context) (*WakatimeDuration, error) {
	nodes, err := wdq.Limit(2).All(setContextOp(ctx, wdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wakatimeduration.Label}
	default:
		return nil, &NotSingularError{wakatimeduration.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) OnlyX(ctx context.Context) *WakatimeDuration {
	node, err := wdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WakatimeDuration ID in the query.
// Returns a *NotSingularError when more than one WakatimeDuration ID is found.
// Returns a *NotFoundError when no entities are found.
func (wdq *WakatimeDurationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wdq.Limit(2).IDs(setContextOp(ctx, wdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wakatimeduration.Label}
	default:
		err = &NotSingularError{wakatimeduration.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) OnlyIDX(ctx context.Context) int {
	id, err := wdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WakatimeDurations.
func (wdq *WakatimeDurationQuery) All(ctx context.Context) ([]*WakatimeDuration, error) {
	ctx = setContextOp(ctx, wdq.ctx, "All")
	if err := wdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WakatimeDuration, *WakatimeDurationQuery]()
	return withInterceptors[[]*WakatimeDuration](ctx, wdq, qr, wdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) AllX(ctx context.Context) []*WakatimeDuration {
	nodes, err := wdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WakatimeDuration IDs.
func (wdq *WakatimeDurationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if wdq.ctx.Unique == nil && wdq.path != nil {
		wdq.Unique(true)
	}
	ctx = setContextOp(ctx, wdq.ctx, "IDs")
	if err = wdq.Select(wakatimeduration.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) IDsX(ctx context.Context) []int {
	ids, err := wdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wdq *WakatimeDurationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wdq.ctx, "Count")
	if err := wdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wdq, querierCount[*WakatimeDurationQuery](), wdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) CountX(ctx context.Context) int {
	count, err := wdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wdq *WakatimeDurationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wdq.ctx, "Exist")
	switch _, err := wdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wdq *WakatimeDurationQuery) ExistX(ctx context.Context) bool {
	exist, err := wdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WakatimeDurationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wdq *WakatimeDurationQuery) Clone() *WakatimeDurationQuery {
	if wdq == nil {
		return nil
	}
	return &WakatimeDurationQuery{
		config:     wdq.config,
		ctx:        wdq.ctx.Clone(),
		order:      append([]wakatimeduration.OrderOption{}, wdq.order...),
		inters:     append([]Interceptor{}, wdq.inters...),
		predicates: append([]predicate.WakatimeDuration{}, wdq.predicates...),
		// clone intermediate query.
		sql:  wdq.sql.Clone(),
		path: wdq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (wdq *WakatimeDurationQuery) GroupBy(field string, fields ...string) *WakatimeDurationGroupBy {
	wdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WakatimeDurationGroupBy{build: wdq}
	grbuild.flds = &wdq.ctx.Fields
	grbuild.label = wakatimeduration.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (wdq *WakatimeDurationQuery) Select(fields ...string) *WakatimeDurationSelect {
	wdq.ctx.Fields = append(wdq.ctx.Fields, fields...)
	sbuild := &WakatimeDurationSelect{WakatimeDurationQuery: wdq}
	sbuild.label = wakatimeduration.Label
	sbuild.flds, sbuild.scan = &wdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WakatimeDurationSelect configured with the given aggregations.
func (wdq *WakatimeDurationQuery) Aggregate(fns ...AggregateFunc) *WakatimeDurationSelect {
	return wdq.Select().Aggregate(fns...)
}

func (wdq *WakatimeDurationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wdq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wdq); err != nil {
				return err
			}
		}
	}
	for _, f := range wdq.ctx.Fields {
		if !wakatimeduration.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if wdq.path != nil {
		prev, err := wdq.path(ctx)
		if err != nil {
			return err
		}
		wdq.sql = prev
	}
	return nil
}

func (wdq *WakatimeDurationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WakatimeDuration, error) {
	var (
		nodes = []*WakatimeDuration{}
		_spec = wdq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WakatimeDuration).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WakatimeDuration{config: wdq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wdq *WakatimeDurationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wdq.querySpec()
	_spec.Node.Columns = wdq.ctx.Fields
	if len(wdq.ctx.Fields) > 0 {
		_spec.Unique = wdq.ctx.Unique != nil && *wdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wdq.driver, _spec)
}

func (wdq *WakatimeDurationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wakatimeduration.Table, wakatimeduration.Columns, sqlgraph.NewFieldSpec(wakatimeduration.FieldID, field.TypeInt))
	_spec.From = wdq.sql
	if unique := wdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wdq.path != nil {
		_spec.Unique = true
	}
	if fields := wdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wakatimeduration.FieldID)
		for i := range fields {
			if fields[i] != wakatimeduration.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wdq *WakatimeDurationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wdq.driver.Dialect())
	t1 := builder.Table(wakatimeduration.Table)
	columns := wdq.ctx.Fields
	if len(columns) == 0 {
		columns = wakatimeduration.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wdq.sql != nil {
		selector = wdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wdq.ctx.Unique != nil && *wdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wdq.predicates {
		p(selector)
	}
	for _, p := range wdq.order {
		p(selector)
	}
	if offset := wdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WakatimeDurationGroupBy is the group-by builder for WakatimeDuration entities.
type WakatimeDurationGroupBy struct {
	selector
	build *WakatimeDurationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wdgb *WakatimeDurationGroupBy) Aggregate(fns ...AggregateFunc) *WakatimeDurationGroupBy {
	wdgb.fns = append(wdgb.fns, fns...)
	return wdgb
}

// Scan applies the selector query and scans the result into the given value.
func (wdgb *WakatimeDurationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wdgb.build.ctx, "GroupBy")
	if err := wdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeDurationQuery, *WakatimeDurationGroupBy](ctx, wdgb.build, wdgb, wdgb.build.inters, v)
}

func (wdgb *WakatimeDurationGroupBy) sqlScan(ctx context.Context, root *WakatimeDurationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wdgb.fns))
	for _, fn := range wdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wdgb.flds)+len(wdgb.fns))
		for _, f := range *wdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WakatimeDurationSelect is the builder for selecting fields of WakatimeDuration entities.
type WakatimeDurationSelect struct {
	*WakatimeDurationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wds *WakatimeDurationSelect) Aggregate(fns ...AggregateFunc) *WakatimeDurationSelect {
	wds.fns = append(wds.fns, fns...)
	return wds
}

// Scan applies the selector query and scans the result into the given value.
func (wds *WakatimeDurationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wds.ctx, "Select")
	if err := wds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeDurationQuery, *WakatimeDurationSelect](ctx, wds.WakatimeDurationQuery, wds, wds.inters, v)
}

func (wds *WakatimeDurationSelect) sqlScan(ctx context.Context, root *WakatimeDurationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wds.fns))
	for _, fn := range wds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
