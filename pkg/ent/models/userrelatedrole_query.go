// Code generated by ent, DO NOT EDIT.

package models

import (
	"context"
	"ent/models/predicate"
	"ent/models/userrelatedrole"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserRelatedRoleQuery is the builder for querying UserRelatedRole entities.
type UserRelatedRoleQuery struct {
	config
	ctx        *QueryContext
	order      []userrelatedrole.OrderOption
	inters     []Interceptor
	predicates []predicate.UserRelatedRole
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserRelatedRoleQuery builder.
func (urrq *UserRelatedRoleQuery) Where(ps ...predicate.UserRelatedRole) *UserRelatedRoleQuery {
	urrq.predicates = append(urrq.predicates, ps...)
	return urrq
}

// Limit the number of records to be returned by this query.
func (urrq *UserRelatedRoleQuery) Limit(limit int) *UserRelatedRoleQuery {
	urrq.ctx.Limit = &limit
	return urrq
}

// Offset to start from.
func (urrq *UserRelatedRoleQuery) Offset(offset int) *UserRelatedRoleQuery {
	urrq.ctx.Offset = &offset
	return urrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (urrq *UserRelatedRoleQuery) Unique(unique bool) *UserRelatedRoleQuery {
	urrq.ctx.Unique = &unique
	return urrq
}

// Order specifies how the records should be ordered.
func (urrq *UserRelatedRoleQuery) Order(o ...userrelatedrole.OrderOption) *UserRelatedRoleQuery {
	urrq.order = append(urrq.order, o...)
	return urrq
}

// First returns the first UserRelatedRole entity from the query.
// Returns a *NotFoundError when no UserRelatedRole was found.
func (urrq *UserRelatedRoleQuery) First(ctx context.Context) (*UserRelatedRole, error) {
	nodes, err := urrq.Limit(1).All(setContextOp(ctx, urrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userrelatedrole.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) FirstX(ctx context.Context) *UserRelatedRole {
	node, err := urrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserRelatedRole ID from the query.
// Returns a *NotFoundError when no UserRelatedRole ID was found.
func (urrq *UserRelatedRoleQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = urrq.Limit(1).IDs(setContextOp(ctx, urrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userrelatedrole.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) FirstIDX(ctx context.Context) int {
	id, err := urrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserRelatedRole entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserRelatedRole entity is found.
// Returns a *NotFoundError when no UserRelatedRole entities are found.
func (urrq *UserRelatedRoleQuery) Only(ctx context.Context) (*UserRelatedRole, error) {
	nodes, err := urrq.Limit(2).All(setContextOp(ctx, urrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userrelatedrole.Label}
	default:
		return nil, &NotSingularError{userrelatedrole.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) OnlyX(ctx context.Context) *UserRelatedRole {
	node, err := urrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserRelatedRole ID in the query.
// Returns a *NotSingularError when more than one UserRelatedRole ID is found.
// Returns a *NotFoundError when no entities are found.
func (urrq *UserRelatedRoleQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = urrq.Limit(2).IDs(setContextOp(ctx, urrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userrelatedrole.Label}
	default:
		err = &NotSingularError{userrelatedrole.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) OnlyIDX(ctx context.Context) int {
	id, err := urrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserRelatedRoles.
func (urrq *UserRelatedRoleQuery) All(ctx context.Context) ([]*UserRelatedRole, error) {
	ctx = setContextOp(ctx, urrq.ctx, "All")
	if err := urrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserRelatedRole, *UserRelatedRoleQuery]()
	return withInterceptors[[]*UserRelatedRole](ctx, urrq, qr, urrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) AllX(ctx context.Context) []*UserRelatedRole {
	nodes, err := urrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserRelatedRole IDs.
func (urrq *UserRelatedRoleQuery) IDs(ctx context.Context) (ids []int, err error) {
	if urrq.ctx.Unique == nil && urrq.path != nil {
		urrq.Unique(true)
	}
	ctx = setContextOp(ctx, urrq.ctx, "IDs")
	if err = urrq.Select(userrelatedrole.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) IDsX(ctx context.Context) []int {
	ids, err := urrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (urrq *UserRelatedRoleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, urrq.ctx, "Count")
	if err := urrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, urrq, querierCount[*UserRelatedRoleQuery](), urrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) CountX(ctx context.Context) int {
	count, err := urrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (urrq *UserRelatedRoleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, urrq.ctx, "Exist")
	switch _, err := urrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("models: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (urrq *UserRelatedRoleQuery) ExistX(ctx context.Context) bool {
	exist, err := urrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserRelatedRoleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (urrq *UserRelatedRoleQuery) Clone() *UserRelatedRoleQuery {
	if urrq == nil {
		return nil
	}
	return &UserRelatedRoleQuery{
		config:     urrq.config,
		ctx:        urrq.ctx.Clone(),
		order:      append([]userrelatedrole.OrderOption{}, urrq.order...),
		inters:     append([]Interceptor{}, urrq.inters...),
		predicates: append([]predicate.UserRelatedRole{}, urrq.predicates...),
		// clone intermediate query.
		sql:  urrq.sql.Clone(),
		path: urrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserRelatedRole.Query().
//		GroupBy(userrelatedrole.FieldUserID).
//		Aggregate(models.Count()).
//		Scan(ctx, &v)
func (urrq *UserRelatedRoleQuery) GroupBy(field string, fields ...string) *UserRelatedRoleGroupBy {
	urrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserRelatedRoleGroupBy{build: urrq}
	grbuild.flds = &urrq.ctx.Fields
	grbuild.label = userrelatedrole.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.UserRelatedRole.Query().
//		Select(userrelatedrole.FieldUserID).
//		Scan(ctx, &v)
func (urrq *UserRelatedRoleQuery) Select(fields ...string) *UserRelatedRoleSelect {
	urrq.ctx.Fields = append(urrq.ctx.Fields, fields...)
	sbuild := &UserRelatedRoleSelect{UserRelatedRoleQuery: urrq}
	sbuild.label = userrelatedrole.Label
	sbuild.flds, sbuild.scan = &urrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserRelatedRoleSelect configured with the given aggregations.
func (urrq *UserRelatedRoleQuery) Aggregate(fns ...AggregateFunc) *UserRelatedRoleSelect {
	return urrq.Select().Aggregate(fns...)
}

func (urrq *UserRelatedRoleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range urrq.inters {
		if inter == nil {
			return fmt.Errorf("models: uninitialized interceptor (forgotten import models/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, urrq); err != nil {
				return err
			}
		}
	}
	for _, f := range urrq.ctx.Fields {
		if !userrelatedrole.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("models: invalid field %q for query", f)}
		}
	}
	if urrq.path != nil {
		prev, err := urrq.path(ctx)
		if err != nil {
			return err
		}
		urrq.sql = prev
	}
	return nil
}

func (urrq *UserRelatedRoleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserRelatedRole, error) {
	var (
		nodes = []*UserRelatedRole{}
		_spec = urrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserRelatedRole).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserRelatedRole{config: urrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, urrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (urrq *UserRelatedRoleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := urrq.querySpec()
	_spec.Node.Columns = urrq.ctx.Fields
	if len(urrq.ctx.Fields) > 0 {
		_spec.Unique = urrq.ctx.Unique != nil && *urrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, urrq.driver, _spec)
}

func (urrq *UserRelatedRoleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userrelatedrole.Table, userrelatedrole.Columns, sqlgraph.NewFieldSpec(userrelatedrole.FieldID, field.TypeInt))
	_spec.From = urrq.sql
	if unique := urrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if urrq.path != nil {
		_spec.Unique = true
	}
	if fields := urrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userrelatedrole.FieldID)
		for i := range fields {
			if fields[i] != userrelatedrole.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := urrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := urrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := urrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := urrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (urrq *UserRelatedRoleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(urrq.driver.Dialect())
	t1 := builder.Table(userrelatedrole.Table)
	columns := urrq.ctx.Fields
	if len(columns) == 0 {
		columns = userrelatedrole.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if urrq.sql != nil {
		selector = urrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if urrq.ctx.Unique != nil && *urrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range urrq.predicates {
		p(selector)
	}
	for _, p := range urrq.order {
		p(selector)
	}
	if offset := urrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := urrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserRelatedRoleGroupBy is the group-by builder for UserRelatedRole entities.
type UserRelatedRoleGroupBy struct {
	selector
	build *UserRelatedRoleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (urrgb *UserRelatedRoleGroupBy) Aggregate(fns ...AggregateFunc) *UserRelatedRoleGroupBy {
	urrgb.fns = append(urrgb.fns, fns...)
	return urrgb
}

// Scan applies the selector query and scans the result into the given value.
func (urrgb *UserRelatedRoleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, urrgb.build.ctx, "GroupBy")
	if err := urrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserRelatedRoleQuery, *UserRelatedRoleGroupBy](ctx, urrgb.build, urrgb, urrgb.build.inters, v)
}

func (urrgb *UserRelatedRoleGroupBy) sqlScan(ctx context.Context, root *UserRelatedRoleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(urrgb.fns))
	for _, fn := range urrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*urrgb.flds)+len(urrgb.fns))
		for _, f := range *urrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*urrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := urrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserRelatedRoleSelect is the builder for selecting fields of UserRelatedRole entities.
type UserRelatedRoleSelect struct {
	*UserRelatedRoleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (urrs *UserRelatedRoleSelect) Aggregate(fns ...AggregateFunc) *UserRelatedRoleSelect {
	urrs.fns = append(urrs.fns, fns...)
	return urrs
}

// Scan applies the selector query and scans the result into the given value.
func (urrs *UserRelatedRoleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, urrs.ctx, "Select")
	if err := urrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserRelatedRoleQuery, *UserRelatedRoleSelect](ctx, urrs.UserRelatedRoleQuery, urrs, urrs.inters, v)
}

func (urrs *UserRelatedRoleSelect) sqlScan(ctx context.Context, root *UserRelatedRoleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(urrs.fns))
	for _, fn := range urrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*urrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := urrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
