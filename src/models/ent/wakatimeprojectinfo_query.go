// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"service-api/src/models/ent/predicate"
	"service-api/src/models/ent/wakatimeprojectinfo"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeProjectInfoQuery is the builder for querying WakatimeProjectInfo entities.
type WakatimeProjectInfoQuery struct {
	config
	ctx        *QueryContext
	order      []wakatimeprojectinfo.OrderOption
	inters     []Interceptor
	predicates []predicate.WakatimeProjectInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WakatimeProjectInfoQuery builder.
func (wpiq *WakatimeProjectInfoQuery) Where(ps ...predicate.WakatimeProjectInfo) *WakatimeProjectInfoQuery {
	wpiq.predicates = append(wpiq.predicates, ps...)
	return wpiq
}

// Limit the number of records to be returned by this query.
func (wpiq *WakatimeProjectInfoQuery) Limit(limit int) *WakatimeProjectInfoQuery {
	wpiq.ctx.Limit = &limit
	return wpiq
}

// Offset to start from.
func (wpiq *WakatimeProjectInfoQuery) Offset(offset int) *WakatimeProjectInfoQuery {
	wpiq.ctx.Offset = &offset
	return wpiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wpiq *WakatimeProjectInfoQuery) Unique(unique bool) *WakatimeProjectInfoQuery {
	wpiq.ctx.Unique = &unique
	return wpiq
}

// Order specifies how the records should be ordered.
func (wpiq *WakatimeProjectInfoQuery) Order(o ...wakatimeprojectinfo.OrderOption) *WakatimeProjectInfoQuery {
	wpiq.order = append(wpiq.order, o...)
	return wpiq
}

// First returns the first WakatimeProjectInfo entity from the query.
// Returns a *NotFoundError when no WakatimeProjectInfo was found.
func (wpiq *WakatimeProjectInfoQuery) First(ctx context.Context) (*WakatimeProjectInfo, error) {
	nodes, err := wpiq.Limit(1).All(setContextOp(ctx, wpiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wakatimeprojectinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) FirstX(ctx context.Context) *WakatimeProjectInfo {
	node, err := wpiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WakatimeProjectInfo ID from the query.
// Returns a *NotFoundError when no WakatimeProjectInfo ID was found.
func (wpiq *WakatimeProjectInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wpiq.Limit(1).IDs(setContextOp(ctx, wpiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wakatimeprojectinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := wpiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WakatimeProjectInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WakatimeProjectInfo entity is found.
// Returns a *NotFoundError when no WakatimeProjectInfo entities are found.
func (wpiq *WakatimeProjectInfoQuery) Only(ctx context.Context) (*WakatimeProjectInfo, error) {
	nodes, err := wpiq.Limit(2).All(setContextOp(ctx, wpiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wakatimeprojectinfo.Label}
	default:
		return nil, &NotSingularError{wakatimeprojectinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) OnlyX(ctx context.Context) *WakatimeProjectInfo {
	node, err := wpiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WakatimeProjectInfo ID in the query.
// Returns a *NotSingularError when more than one WakatimeProjectInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (wpiq *WakatimeProjectInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wpiq.Limit(2).IDs(setContextOp(ctx, wpiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wakatimeprojectinfo.Label}
	default:
		err = &NotSingularError{wakatimeprojectinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := wpiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WakatimeProjectInfos.
func (wpiq *WakatimeProjectInfoQuery) All(ctx context.Context) ([]*WakatimeProjectInfo, error) {
	ctx = setContextOp(ctx, wpiq.ctx, "All")
	if err := wpiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WakatimeProjectInfo, *WakatimeProjectInfoQuery]()
	return withInterceptors[[]*WakatimeProjectInfo](ctx, wpiq, qr, wpiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) AllX(ctx context.Context) []*WakatimeProjectInfo {
	nodes, err := wpiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WakatimeProjectInfo IDs.
func (wpiq *WakatimeProjectInfoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if wpiq.ctx.Unique == nil && wpiq.path != nil {
		wpiq.Unique(true)
	}
	ctx = setContextOp(ctx, wpiq.ctx, "IDs")
	if err = wpiq.Select(wakatimeprojectinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := wpiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wpiq *WakatimeProjectInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wpiq.ctx, "Count")
	if err := wpiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wpiq, querierCount[*WakatimeProjectInfoQuery](), wpiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) CountX(ctx context.Context) int {
	count, err := wpiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wpiq *WakatimeProjectInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wpiq.ctx, "Exist")
	switch _, err := wpiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wpiq *WakatimeProjectInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := wpiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WakatimeProjectInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wpiq *WakatimeProjectInfoQuery) Clone() *WakatimeProjectInfoQuery {
	if wpiq == nil {
		return nil
	}
	return &WakatimeProjectInfoQuery{
		config:     wpiq.config,
		ctx:        wpiq.ctx.Clone(),
		order:      append([]wakatimeprojectinfo.OrderOption{}, wpiq.order...),
		inters:     append([]Interceptor{}, wpiq.inters...),
		predicates: append([]predicate.WakatimeProjectInfo{}, wpiq.predicates...),
		// clone intermediate query.
		sql:  wpiq.sql.Clone(),
		path: wpiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (wpiq *WakatimeProjectInfoQuery) GroupBy(field string, fields ...string) *WakatimeProjectInfoGroupBy {
	wpiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WakatimeProjectInfoGroupBy{build: wpiq}
	grbuild.flds = &wpiq.ctx.Fields
	grbuild.label = wakatimeprojectinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (wpiq *WakatimeProjectInfoQuery) Select(fields ...string) *WakatimeProjectInfoSelect {
	wpiq.ctx.Fields = append(wpiq.ctx.Fields, fields...)
	sbuild := &WakatimeProjectInfoSelect{WakatimeProjectInfoQuery: wpiq}
	sbuild.label = wakatimeprojectinfo.Label
	sbuild.flds, sbuild.scan = &wpiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WakatimeProjectInfoSelect configured with the given aggregations.
func (wpiq *WakatimeProjectInfoQuery) Aggregate(fns ...AggregateFunc) *WakatimeProjectInfoSelect {
	return wpiq.Select().Aggregate(fns...)
}

func (wpiq *WakatimeProjectInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wpiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wpiq); err != nil {
				return err
			}
		}
	}
	for _, f := range wpiq.ctx.Fields {
		if !wakatimeprojectinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wpiq.path != nil {
		prev, err := wpiq.path(ctx)
		if err != nil {
			return err
		}
		wpiq.sql = prev
	}
	return nil
}

func (wpiq *WakatimeProjectInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WakatimeProjectInfo, error) {
	var (
		nodes = []*WakatimeProjectInfo{}
		_spec = wpiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WakatimeProjectInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WakatimeProjectInfo{config: wpiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wpiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wpiq *WakatimeProjectInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wpiq.querySpec()
	_spec.Node.Columns = wpiq.ctx.Fields
	if len(wpiq.ctx.Fields) > 0 {
		_spec.Unique = wpiq.ctx.Unique != nil && *wpiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wpiq.driver, _spec)
}

func (wpiq *WakatimeProjectInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wakatimeprojectinfo.Table, wakatimeprojectinfo.Columns, sqlgraph.NewFieldSpec(wakatimeprojectinfo.FieldID, field.TypeInt))
	_spec.From = wpiq.sql
	if unique := wpiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wpiq.path != nil {
		_spec.Unique = true
	}
	if fields := wpiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wakatimeprojectinfo.FieldID)
		for i := range fields {
			if fields[i] != wakatimeprojectinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wpiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wpiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wpiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wpiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wpiq *WakatimeProjectInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wpiq.driver.Dialect())
	t1 := builder.Table(wakatimeprojectinfo.Table)
	columns := wpiq.ctx.Fields
	if len(columns) == 0 {
		columns = wakatimeprojectinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wpiq.sql != nil {
		selector = wpiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wpiq.ctx.Unique != nil && *wpiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wpiq.predicates {
		p(selector)
	}
	for _, p := range wpiq.order {
		p(selector)
	}
	if offset := wpiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wpiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WakatimeProjectInfoGroupBy is the group-by builder for WakatimeProjectInfo entities.
type WakatimeProjectInfoGroupBy struct {
	selector
	build *WakatimeProjectInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wpigb *WakatimeProjectInfoGroupBy) Aggregate(fns ...AggregateFunc) *WakatimeProjectInfoGroupBy {
	wpigb.fns = append(wpigb.fns, fns...)
	return wpigb
}

// Scan applies the selector query and scans the result into the given value.
func (wpigb *WakatimeProjectInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wpigb.build.ctx, "GroupBy")
	if err := wpigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeProjectInfoQuery, *WakatimeProjectInfoGroupBy](ctx, wpigb.build, wpigb, wpigb.build.inters, v)
}

func (wpigb *WakatimeProjectInfoGroupBy) sqlScan(ctx context.Context, root *WakatimeProjectInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wpigb.fns))
	for _, fn := range wpigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wpigb.flds)+len(wpigb.fns))
		for _, f := range *wpigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wpigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wpigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WakatimeProjectInfoSelect is the builder for selecting fields of WakatimeProjectInfo entities.
type WakatimeProjectInfoSelect struct {
	*WakatimeProjectInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wpis *WakatimeProjectInfoSelect) Aggregate(fns ...AggregateFunc) *WakatimeProjectInfoSelect {
	wpis.fns = append(wpis.fns, fns...)
	return wpis
}

// Scan applies the selector query and scans the result into the given value.
func (wpis *WakatimeProjectInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wpis.ctx, "Select")
	if err := wpis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeProjectInfoQuery, *WakatimeProjectInfoSelect](ctx, wpis.WakatimeProjectInfoQuery, wpis, wpis.inters, v)
}

func (wpis *WakatimeProjectInfoSelect) sqlScan(ctx context.Context, root *WakatimeProjectInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wpis.fns))
	for _, fn := range wpis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wpis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wpis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
