// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"service-api/src/models/ent/wakatimeproject"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeProjectCreate is the builder for creating a WakatimeProject entity.
type WakatimeProjectCreate struct {
	config
	mutation *WakatimeProjectMutation
	hooks    []Hook
}

// Mutation returns the WakatimeProjectMutation object of the builder.
func (wpc *WakatimeProjectCreate) Mutation() *WakatimeProjectMutation {
	return wpc.mutation
}

// Save creates the WakatimeProject in the database.
func (wpc *WakatimeProjectCreate) Save(ctx context.Context) (*WakatimeProject, error) {
	return withHooks(ctx, wpc.sqlSave, wpc.mutation, wpc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (wpc *WakatimeProjectCreate) SaveX(ctx context.Context) *WakatimeProject {
	v, err := wpc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wpc *WakatimeProjectCreate) Exec(ctx context.Context) error {
	_, err := wpc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wpc *WakatimeProjectCreate) ExecX(ctx context.Context) {
	if err := wpc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wpc *WakatimeProjectCreate) check() error {
	return nil
}

func (wpc *WakatimeProjectCreate) sqlSave(ctx context.Context) (*WakatimeProject, error) {
	if err := wpc.check(); err != nil {
		return nil, err
	}
	_node, _spec := wpc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wpc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	wpc.mutation.id = &_node.ID
	wpc.mutation.done = true
	return _node, nil
}

func (wpc *WakatimeProjectCreate) createSpec() (*WakatimeProject, *sqlgraph.CreateSpec) {
	var (
		_node = &WakatimeProject{config: wpc.config}
		_spec = sqlgraph.NewCreateSpec(wakatimeproject.Table, sqlgraph.NewFieldSpec(wakatimeproject.FieldID, field.TypeInt))
	)
	return _node, _spec
}

// WakatimeProjectCreateBulk is the builder for creating many WakatimeProject entities in bulk.
type WakatimeProjectCreateBulk struct {
	config
	err      error
	builders []*WakatimeProjectCreate
}

// Save creates the WakatimeProject entities in the database.
func (wpcb *WakatimeProjectCreateBulk) Save(ctx context.Context) ([]*WakatimeProject, error) {
	if wpcb.err != nil {
		return nil, wpcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(wpcb.builders))
	nodes := make([]*WakatimeProject, len(wpcb.builders))
	mutators := make([]Mutator, len(wpcb.builders))
	for i := range wpcb.builders {
		func(i int, root context.Context) {
			builder := wpcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WakatimeProjectMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wpcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wpcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wpcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (wpcb *WakatimeProjectCreateBulk) SaveX(ctx context.Context) []*WakatimeProject {
	v, err := wpcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wpcb *WakatimeProjectCreateBulk) Exec(ctx context.Context) error {
	_, err := wpcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wpcb *WakatimeProjectCreateBulk) ExecX(ctx context.Context) {
	if err := wpcb.Exec(ctx); err != nil {
		panic(err)
	}
}
