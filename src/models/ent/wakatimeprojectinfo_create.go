// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"service-api/src/models/ent/wakatimeprojectinfo"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeProjectInfoCreate is the builder for creating a WakatimeProjectInfo entity.
type WakatimeProjectInfoCreate struct {
	config
	mutation *WakatimeProjectInfoMutation
	hooks    []Hook
}

// Mutation returns the WakatimeProjectInfoMutation object of the builder.
func (wpic *WakatimeProjectInfoCreate) Mutation() *WakatimeProjectInfoMutation {
	return wpic.mutation
}

// Save creates the WakatimeProjectInfo in the database.
func (wpic *WakatimeProjectInfoCreate) Save(ctx context.Context) (*WakatimeProjectInfo, error) {
	return withHooks(ctx, wpic.sqlSave, wpic.mutation, wpic.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (wpic *WakatimeProjectInfoCreate) SaveX(ctx context.Context) *WakatimeProjectInfo {
	v, err := wpic.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wpic *WakatimeProjectInfoCreate) Exec(ctx context.Context) error {
	_, err := wpic.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wpic *WakatimeProjectInfoCreate) ExecX(ctx context.Context) {
	if err := wpic.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wpic *WakatimeProjectInfoCreate) check() error {
	return nil
}

func (wpic *WakatimeProjectInfoCreate) sqlSave(ctx context.Context) (*WakatimeProjectInfo, error) {
	if err := wpic.check(); err != nil {
		return nil, err
	}
	_node, _spec := wpic.createSpec()
	if err := sqlgraph.CreateNode(ctx, wpic.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	wpic.mutation.id = &_node.ID
	wpic.mutation.done = true
	return _node, nil
}

func (wpic *WakatimeProjectInfoCreate) createSpec() (*WakatimeProjectInfo, *sqlgraph.CreateSpec) {
	var (
		_node = &WakatimeProjectInfo{config: wpic.config}
		_spec = sqlgraph.NewCreateSpec(wakatimeprojectinfo.Table, sqlgraph.NewFieldSpec(wakatimeprojectinfo.FieldID, field.TypeInt))
	)
	return _node, _spec
}

// WakatimeProjectInfoCreateBulk is the builder for creating many WakatimeProjectInfo entities in bulk.
type WakatimeProjectInfoCreateBulk struct {
	config
	err      error
	builders []*WakatimeProjectInfoCreate
}

// Save creates the WakatimeProjectInfo entities in the database.
func (wpicb *WakatimeProjectInfoCreateBulk) Save(ctx context.Context) ([]*WakatimeProjectInfo, error) {
	if wpicb.err != nil {
		return nil, wpicb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(wpicb.builders))
	nodes := make([]*WakatimeProjectInfo, len(wpicb.builders))
	mutators := make([]Mutator, len(wpicb.builders))
	for i := range wpicb.builders {
		func(i int, root context.Context) {
			builder := wpicb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WakatimeProjectInfoMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wpicb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wpicb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wpicb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (wpicb *WakatimeProjectInfoCreateBulk) SaveX(ctx context.Context) []*WakatimeProjectInfo {
	v, err := wpicb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wpicb *WakatimeProjectInfoCreateBulk) Exec(ctx context.Context) error {
	_, err := wpicb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wpicb *WakatimeProjectInfoCreateBulk) ExecX(ctx context.Context) {
	if err := wpicb.Exec(ctx); err != nil {
		panic(err)
	}
}
