// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"service-api/src/models/ent/memberrelatedrole"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// MemberRelatedRole is the model entity for the MemberRelatedRole schema.
type MemberRelatedRole struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// 会员id
	MemberID uuid.UUID `json:"member_id,omitempty"`
	// 角色
	RoleID       uuid.UUID `json:"role_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MemberRelatedRole) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case memberrelatedrole.FieldCreateTime, memberrelatedrole.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case memberrelatedrole.FieldID, memberrelatedrole.FieldMemberID, memberrelatedrole.FieldRoleID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MemberRelatedRole fields.
func (mrr *MemberRelatedRole) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case memberrelatedrole.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				mrr.ID = *value
			}
		case memberrelatedrole.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				mrr.CreateTime = value.Time
			}
		case memberrelatedrole.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				mrr.UpdateTime = value.Time
			}
		case memberrelatedrole.FieldMemberID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field member_id", values[i])
			} else if value != nil {
				mrr.MemberID = *value
			}
		case memberrelatedrole.FieldRoleID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field role_id", values[i])
			} else if value != nil {
				mrr.RoleID = *value
			}
		default:
			mrr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MemberRelatedRole.
// This includes values selected through modifiers, order, etc.
func (mrr *MemberRelatedRole) Value(name string) (ent.Value, error) {
	return mrr.selectValues.Get(name)
}

// Update returns a builder for updating this MemberRelatedRole.
// Note that you need to call MemberRelatedRole.Unwrap() before calling this method if this MemberRelatedRole
// was returned from a transaction, and the transaction was committed or rolled back.
func (mrr *MemberRelatedRole) Update() *MemberRelatedRoleUpdateOne {
	return NewMemberRelatedRoleClient(mrr.config).UpdateOne(mrr)
}

// Unwrap unwraps the MemberRelatedRole entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mrr *MemberRelatedRole) Unwrap() *MemberRelatedRole {
	_tx, ok := mrr.config.driver.(*txDriver)
	if !ok {
		panic("ent: MemberRelatedRole is not a transactional entity")
	}
	mrr.config.driver = _tx.drv
	return mrr
}

// String implements the fmt.Stringer.
func (mrr *MemberRelatedRole) String() string {
	var builder strings.Builder
	builder.WriteString("MemberRelatedRole(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mrr.ID))
	builder.WriteString("create_time=")
	builder.WriteString(mrr.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(mrr.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("member_id=")
	builder.WriteString(fmt.Sprintf("%v", mrr.MemberID))
	builder.WriteString(", ")
	builder.WriteString("role_id=")
	builder.WriteString(fmt.Sprintf("%v", mrr.RoleID))
	builder.WriteByte(')')
	return builder.String()
}

// MemberRelatedRoles is a parsable slice of MemberRelatedRole.
type MemberRelatedRoles []*MemberRelatedRole
