// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"service-api/src/models/ent/predicate"
	"service-api/src/models/ent/wakatimeeditor"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeEditorQuery is the builder for querying WakatimeEditor entities.
type WakatimeEditorQuery struct {
	config
	ctx        *QueryContext
	order      []wakatimeeditor.OrderOption
	inters     []Interceptor
	predicates []predicate.WakatimeEditor
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WakatimeEditorQuery builder.
func (weq *WakatimeEditorQuery) Where(ps ...predicate.WakatimeEditor) *WakatimeEditorQuery {
	weq.predicates = append(weq.predicates, ps...)
	return weq
}

// Limit the number of records to be returned by this query.
func (weq *WakatimeEditorQuery) Limit(limit int) *WakatimeEditorQuery {
	weq.ctx.Limit = &limit
	return weq
}

// Offset to start from.
func (weq *WakatimeEditorQuery) Offset(offset int) *WakatimeEditorQuery {
	weq.ctx.Offset = &offset
	return weq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (weq *WakatimeEditorQuery) Unique(unique bool) *WakatimeEditorQuery {
	weq.ctx.Unique = &unique
	return weq
}

// Order specifies how the records should be ordered.
func (weq *WakatimeEditorQuery) Order(o ...wakatimeeditor.OrderOption) *WakatimeEditorQuery {
	weq.order = append(weq.order, o...)
	return weq
}

// First returns the first WakatimeEditor entity from the query.
// Returns a *NotFoundError when no WakatimeEditor was found.
func (weq *WakatimeEditorQuery) First(ctx context.Context) (*WakatimeEditor, error) {
	nodes, err := weq.Limit(1).All(setContextOp(ctx, weq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wakatimeeditor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (weq *WakatimeEditorQuery) FirstX(ctx context.Context) *WakatimeEditor {
	node, err := weq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WakatimeEditor ID from the query.
// Returns a *NotFoundError when no WakatimeEditor ID was found.
func (weq *WakatimeEditorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = weq.Limit(1).IDs(setContextOp(ctx, weq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wakatimeeditor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (weq *WakatimeEditorQuery) FirstIDX(ctx context.Context) int {
	id, err := weq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WakatimeEditor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WakatimeEditor entity is found.
// Returns a *NotFoundError when no WakatimeEditor entities are found.
func (weq *WakatimeEditorQuery) Only(ctx context.Context) (*WakatimeEditor, error) {
	nodes, err := weq.Limit(2).All(setContextOp(ctx, weq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wakatimeeditor.Label}
	default:
		return nil, &NotSingularError{wakatimeeditor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (weq *WakatimeEditorQuery) OnlyX(ctx context.Context) *WakatimeEditor {
	node, err := weq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WakatimeEditor ID in the query.
// Returns a *NotSingularError when more than one WakatimeEditor ID is found.
// Returns a *NotFoundError when no entities are found.
func (weq *WakatimeEditorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = weq.Limit(2).IDs(setContextOp(ctx, weq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wakatimeeditor.Label}
	default:
		err = &NotSingularError{wakatimeeditor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (weq *WakatimeEditorQuery) OnlyIDX(ctx context.Context) int {
	id, err := weq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WakatimeEditors.
func (weq *WakatimeEditorQuery) All(ctx context.Context) ([]*WakatimeEditor, error) {
	ctx = setContextOp(ctx, weq.ctx, "All")
	if err := weq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WakatimeEditor, *WakatimeEditorQuery]()
	return withInterceptors[[]*WakatimeEditor](ctx, weq, qr, weq.inters)
}

// AllX is like All, but panics if an error occurs.
func (weq *WakatimeEditorQuery) AllX(ctx context.Context) []*WakatimeEditor {
	nodes, err := weq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WakatimeEditor IDs.
func (weq *WakatimeEditorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if weq.ctx.Unique == nil && weq.path != nil {
		weq.Unique(true)
	}
	ctx = setContextOp(ctx, weq.ctx, "IDs")
	if err = weq.Select(wakatimeeditor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (weq *WakatimeEditorQuery) IDsX(ctx context.Context) []int {
	ids, err := weq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (weq *WakatimeEditorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, weq.ctx, "Count")
	if err := weq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, weq, querierCount[*WakatimeEditorQuery](), weq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (weq *WakatimeEditorQuery) CountX(ctx context.Context) int {
	count, err := weq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (weq *WakatimeEditorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, weq.ctx, "Exist")
	switch _, err := weq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (weq *WakatimeEditorQuery) ExistX(ctx context.Context) bool {
	exist, err := weq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WakatimeEditorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (weq *WakatimeEditorQuery) Clone() *WakatimeEditorQuery {
	if weq == nil {
		return nil
	}
	return &WakatimeEditorQuery{
		config:     weq.config,
		ctx:        weq.ctx.Clone(),
		order:      append([]wakatimeeditor.OrderOption{}, weq.order...),
		inters:     append([]Interceptor{}, weq.inters...),
		predicates: append([]predicate.WakatimeEditor{}, weq.predicates...),
		// clone intermediate query.
		sql:  weq.sql.Clone(),
		path: weq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (weq *WakatimeEditorQuery) GroupBy(field string, fields ...string) *WakatimeEditorGroupBy {
	weq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WakatimeEditorGroupBy{build: weq}
	grbuild.flds = &weq.ctx.Fields
	grbuild.label = wakatimeeditor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (weq *WakatimeEditorQuery) Select(fields ...string) *WakatimeEditorSelect {
	weq.ctx.Fields = append(weq.ctx.Fields, fields...)
	sbuild := &WakatimeEditorSelect{WakatimeEditorQuery: weq}
	sbuild.label = wakatimeeditor.Label
	sbuild.flds, sbuild.scan = &weq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WakatimeEditorSelect configured with the given aggregations.
func (weq *WakatimeEditorQuery) Aggregate(fns ...AggregateFunc) *WakatimeEditorSelect {
	return weq.Select().Aggregate(fns...)
}

func (weq *WakatimeEditorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range weq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, weq); err != nil {
				return err
			}
		}
	}
	for _, f := range weq.ctx.Fields {
		if !wakatimeeditor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if weq.path != nil {
		prev, err := weq.path(ctx)
		if err != nil {
			return err
		}
		weq.sql = prev
	}
	return nil
}

func (weq *WakatimeEditorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WakatimeEditor, error) {
	var (
		nodes = []*WakatimeEditor{}
		_spec = weq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WakatimeEditor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WakatimeEditor{config: weq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, weq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (weq *WakatimeEditorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := weq.querySpec()
	_spec.Node.Columns = weq.ctx.Fields
	if len(weq.ctx.Fields) > 0 {
		_spec.Unique = weq.ctx.Unique != nil && *weq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, weq.driver, _spec)
}

func (weq *WakatimeEditorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wakatimeeditor.Table, wakatimeeditor.Columns, sqlgraph.NewFieldSpec(wakatimeeditor.FieldID, field.TypeInt))
	_spec.From = weq.sql
	if unique := weq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if weq.path != nil {
		_spec.Unique = true
	}
	if fields := weq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wakatimeeditor.FieldID)
		for i := range fields {
			if fields[i] != wakatimeeditor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := weq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := weq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := weq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := weq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (weq *WakatimeEditorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(weq.driver.Dialect())
	t1 := builder.Table(wakatimeeditor.Table)
	columns := weq.ctx.Fields
	if len(columns) == 0 {
		columns = wakatimeeditor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if weq.sql != nil {
		selector = weq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if weq.ctx.Unique != nil && *weq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range weq.predicates {
		p(selector)
	}
	for _, p := range weq.order {
		p(selector)
	}
	if offset := weq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := weq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WakatimeEditorGroupBy is the group-by builder for WakatimeEditor entities.
type WakatimeEditorGroupBy struct {
	selector
	build *WakatimeEditorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wegb *WakatimeEditorGroupBy) Aggregate(fns ...AggregateFunc) *WakatimeEditorGroupBy {
	wegb.fns = append(wegb.fns, fns...)
	return wegb
}

// Scan applies the selector query and scans the result into the given value.
func (wegb *WakatimeEditorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wegb.build.ctx, "GroupBy")
	if err := wegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeEditorQuery, *WakatimeEditorGroupBy](ctx, wegb.build, wegb, wegb.build.inters, v)
}

func (wegb *WakatimeEditorGroupBy) sqlScan(ctx context.Context, root *WakatimeEditorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wegb.fns))
	for _, fn := range wegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wegb.flds)+len(wegb.fns))
		for _, f := range *wegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WakatimeEditorSelect is the builder for selecting fields of WakatimeEditor entities.
type WakatimeEditorSelect struct {
	*WakatimeEditorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wes *WakatimeEditorSelect) Aggregate(fns ...AggregateFunc) *WakatimeEditorSelect {
	wes.fns = append(wes.fns, fns...)
	return wes
}

// Scan applies the selector query and scans the result into the given value.
func (wes *WakatimeEditorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wes.ctx, "Select")
	if err := wes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeEditorQuery, *WakatimeEditorSelect](ctx, wes.WakatimeEditorQuery, wes, wes.inters, v)
}

func (wes *WakatimeEditorSelect) sqlScan(ctx context.Context, root *WakatimeEditorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wes.fns))
	for _, fn := range wes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
