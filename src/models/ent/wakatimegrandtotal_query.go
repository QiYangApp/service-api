// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"service-api/src/models/ent/predicate"
	"service-api/src/models/ent/wakatimegrandtotal"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// WakatimeGrandTotalQuery is the builder for querying WakatimeGrandTotal entities.
type WakatimeGrandTotalQuery struct {
	config
	ctx        *QueryContext
	order      []wakatimegrandtotal.OrderOption
	inters     []Interceptor
	predicates []predicate.WakatimeGrandTotal
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WakatimeGrandTotalQuery builder.
func (wgtq *WakatimeGrandTotalQuery) Where(ps ...predicate.WakatimeGrandTotal) *WakatimeGrandTotalQuery {
	wgtq.predicates = append(wgtq.predicates, ps...)
	return wgtq
}

// Limit the number of records to be returned by this query.
func (wgtq *WakatimeGrandTotalQuery) Limit(limit int) *WakatimeGrandTotalQuery {
	wgtq.ctx.Limit = &limit
	return wgtq
}

// Offset to start from.
func (wgtq *WakatimeGrandTotalQuery) Offset(offset int) *WakatimeGrandTotalQuery {
	wgtq.ctx.Offset = &offset
	return wgtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wgtq *WakatimeGrandTotalQuery) Unique(unique bool) *WakatimeGrandTotalQuery {
	wgtq.ctx.Unique = &unique
	return wgtq
}

// Order specifies how the records should be ordered.
func (wgtq *WakatimeGrandTotalQuery) Order(o ...wakatimegrandtotal.OrderOption) *WakatimeGrandTotalQuery {
	wgtq.order = append(wgtq.order, o...)
	return wgtq
}

// First returns the first WakatimeGrandTotal entity from the query.
// Returns a *NotFoundError when no WakatimeGrandTotal was found.
func (wgtq *WakatimeGrandTotalQuery) First(ctx context.Context) (*WakatimeGrandTotal, error) {
	nodes, err := wgtq.Limit(1).All(setContextOp(ctx, wgtq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wakatimegrandtotal.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) FirstX(ctx context.Context) *WakatimeGrandTotal {
	node, err := wgtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WakatimeGrandTotal ID from the query.
// Returns a *NotFoundError when no WakatimeGrandTotal ID was found.
func (wgtq *WakatimeGrandTotalQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wgtq.Limit(1).IDs(setContextOp(ctx, wgtq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wakatimegrandtotal.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) FirstIDX(ctx context.Context) int {
	id, err := wgtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WakatimeGrandTotal entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WakatimeGrandTotal entity is found.
// Returns a *NotFoundError when no WakatimeGrandTotal entities are found.
func (wgtq *WakatimeGrandTotalQuery) Only(ctx context.Context) (*WakatimeGrandTotal, error) {
	nodes, err := wgtq.Limit(2).All(setContextOp(ctx, wgtq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wakatimegrandtotal.Label}
	default:
		return nil, &NotSingularError{wakatimegrandtotal.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) OnlyX(ctx context.Context) *WakatimeGrandTotal {
	node, err := wgtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WakatimeGrandTotal ID in the query.
// Returns a *NotSingularError when more than one WakatimeGrandTotal ID is found.
// Returns a *NotFoundError when no entities are found.
func (wgtq *WakatimeGrandTotalQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wgtq.Limit(2).IDs(setContextOp(ctx, wgtq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wakatimegrandtotal.Label}
	default:
		err = &NotSingularError{wakatimegrandtotal.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) OnlyIDX(ctx context.Context) int {
	id, err := wgtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WakatimeGrandTotals.
func (wgtq *WakatimeGrandTotalQuery) All(ctx context.Context) ([]*WakatimeGrandTotal, error) {
	ctx = setContextOp(ctx, wgtq.ctx, "All")
	if err := wgtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WakatimeGrandTotal, *WakatimeGrandTotalQuery]()
	return withInterceptors[[]*WakatimeGrandTotal](ctx, wgtq, qr, wgtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) AllX(ctx context.Context) []*WakatimeGrandTotal {
	nodes, err := wgtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WakatimeGrandTotal IDs.
func (wgtq *WakatimeGrandTotalQuery) IDs(ctx context.Context) (ids []int, err error) {
	if wgtq.ctx.Unique == nil && wgtq.path != nil {
		wgtq.Unique(true)
	}
	ctx = setContextOp(ctx, wgtq.ctx, "IDs")
	if err = wgtq.Select(wakatimegrandtotal.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) IDsX(ctx context.Context) []int {
	ids, err := wgtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wgtq *WakatimeGrandTotalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wgtq.ctx, "Count")
	if err := wgtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wgtq, querierCount[*WakatimeGrandTotalQuery](), wgtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) CountX(ctx context.Context) int {
	count, err := wgtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wgtq *WakatimeGrandTotalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wgtq.ctx, "Exist")
	switch _, err := wgtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wgtq *WakatimeGrandTotalQuery) ExistX(ctx context.Context) bool {
	exist, err := wgtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WakatimeGrandTotalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wgtq *WakatimeGrandTotalQuery) Clone() *WakatimeGrandTotalQuery {
	if wgtq == nil {
		return nil
	}
	return &WakatimeGrandTotalQuery{
		config:     wgtq.config,
		ctx:        wgtq.ctx.Clone(),
		order:      append([]wakatimegrandtotal.OrderOption{}, wgtq.order...),
		inters:     append([]Interceptor{}, wgtq.inters...),
		predicates: append([]predicate.WakatimeGrandTotal{}, wgtq.predicates...),
		// clone intermediate query.
		sql:  wgtq.sql.Clone(),
		path: wgtq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (wgtq *WakatimeGrandTotalQuery) GroupBy(field string, fields ...string) *WakatimeGrandTotalGroupBy {
	wgtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WakatimeGrandTotalGroupBy{build: wgtq}
	grbuild.flds = &wgtq.ctx.Fields
	grbuild.label = wakatimegrandtotal.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (wgtq *WakatimeGrandTotalQuery) Select(fields ...string) *WakatimeGrandTotalSelect {
	wgtq.ctx.Fields = append(wgtq.ctx.Fields, fields...)
	sbuild := &WakatimeGrandTotalSelect{WakatimeGrandTotalQuery: wgtq}
	sbuild.label = wakatimegrandtotal.Label
	sbuild.flds, sbuild.scan = &wgtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WakatimeGrandTotalSelect configured with the given aggregations.
func (wgtq *WakatimeGrandTotalQuery) Aggregate(fns ...AggregateFunc) *WakatimeGrandTotalSelect {
	return wgtq.Select().Aggregate(fns...)
}

func (wgtq *WakatimeGrandTotalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wgtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wgtq); err != nil {
				return err
			}
		}
	}
	for _, f := range wgtq.ctx.Fields {
		if !wakatimegrandtotal.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wgtq.path != nil {
		prev, err := wgtq.path(ctx)
		if err != nil {
			return err
		}
		wgtq.sql = prev
	}
	return nil
}

func (wgtq *WakatimeGrandTotalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WakatimeGrandTotal, error) {
	var (
		nodes = []*WakatimeGrandTotal{}
		_spec = wgtq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WakatimeGrandTotal).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WakatimeGrandTotal{config: wgtq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wgtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wgtq *WakatimeGrandTotalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wgtq.querySpec()
	_spec.Node.Columns = wgtq.ctx.Fields
	if len(wgtq.ctx.Fields) > 0 {
		_spec.Unique = wgtq.ctx.Unique != nil && *wgtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wgtq.driver, _spec)
}

func (wgtq *WakatimeGrandTotalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wakatimegrandtotal.Table, wakatimegrandtotal.Columns, sqlgraph.NewFieldSpec(wakatimegrandtotal.FieldID, field.TypeInt))
	_spec.From = wgtq.sql
	if unique := wgtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wgtq.path != nil {
		_spec.Unique = true
	}
	if fields := wgtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wakatimegrandtotal.FieldID)
		for i := range fields {
			if fields[i] != wakatimegrandtotal.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wgtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wgtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wgtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wgtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wgtq *WakatimeGrandTotalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wgtq.driver.Dialect())
	t1 := builder.Table(wakatimegrandtotal.Table)
	columns := wgtq.ctx.Fields
	if len(columns) == 0 {
		columns = wakatimegrandtotal.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wgtq.sql != nil {
		selector = wgtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wgtq.ctx.Unique != nil && *wgtq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wgtq.predicates {
		p(selector)
	}
	for _, p := range wgtq.order {
		p(selector)
	}
	if offset := wgtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wgtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WakatimeGrandTotalGroupBy is the group-by builder for WakatimeGrandTotal entities.
type WakatimeGrandTotalGroupBy struct {
	selector
	build *WakatimeGrandTotalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wgtgb *WakatimeGrandTotalGroupBy) Aggregate(fns ...AggregateFunc) *WakatimeGrandTotalGroupBy {
	wgtgb.fns = append(wgtgb.fns, fns...)
	return wgtgb
}

// Scan applies the selector query and scans the result into the given value.
func (wgtgb *WakatimeGrandTotalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wgtgb.build.ctx, "GroupBy")
	if err := wgtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeGrandTotalQuery, *WakatimeGrandTotalGroupBy](ctx, wgtgb.build, wgtgb, wgtgb.build.inters, v)
}

func (wgtgb *WakatimeGrandTotalGroupBy) sqlScan(ctx context.Context, root *WakatimeGrandTotalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wgtgb.fns))
	for _, fn := range wgtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wgtgb.flds)+len(wgtgb.fns))
		for _, f := range *wgtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wgtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wgtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WakatimeGrandTotalSelect is the builder for selecting fields of WakatimeGrandTotal entities.
type WakatimeGrandTotalSelect struct {
	*WakatimeGrandTotalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wgts *WakatimeGrandTotalSelect) Aggregate(fns ...AggregateFunc) *WakatimeGrandTotalSelect {
	wgts.fns = append(wgts.fns, fns...)
	return wgts
}

// Scan applies the selector query and scans the result into the given value.
func (wgts *WakatimeGrandTotalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wgts.ctx, "Select")
	if err := wgts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WakatimeGrandTotalQuery, *WakatimeGrandTotalSelect](ctx, wgts.WakatimeGrandTotalQuery, wgts, wgts.inters, v)
}

func (wgts *WakatimeGrandTotalSelect) sqlScan(ctx context.Context, root *WakatimeGrandTotalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wgts.fns))
	for _, fn := range wgts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wgts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wgts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
